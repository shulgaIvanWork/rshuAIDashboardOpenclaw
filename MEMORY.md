
## 🛑 Критическое правило: разрешение перед реализацией

**Перед началом любой реализации (правка файлов, добавление кода, изменение архитектуры) — СПРАШИВАТЬ РАЗРЕШЕНИЕ у Ивана.**

Не начинать править файлы без явного одобрения. В прошлый раз я удалил авторизацию, роли админов/гостей и опубликованные/в-разработке дашборды, когда меня просто спросили «как сделать» — это было ошибкой.

Порядок действий:
1. Получить задачу от Ивана или коллеги
2. Спросить разрешение: «можно приступить к реализации?»
3. Только после явного «да» — начинать править файлы

## Архитектура: единый сервис на порту 3000

Все дашборды — под-приложения внутри `clover-web` (Express.js на порту 3000).
Никаких отдельных портов и PM2 процессов для дашбордов не нужно.
Новый дашборд = новая папка с server.js + mount в web-interface/server.js.

## Источники данных (2 основных)

1. **Bitrix24 REST API** — вебхук `https://24.uprav.ru/rest/516/k1cdomfp4vd1kiql/`
2. **Яндекс Метрика API** — дубликат в TOOLS.md

### 2. Яндекс Метрика API — учётные данные

**ClientID:** `78b1e8c8046f4b08837f3d007ac983b4`
**Client secret:** `055582f270bc4443984e42e6020d68f0`
**Токен OAuth:** `y0__wgBELrbs5oCGKr3QiDt4u3iF6ydZv9PW4NDN8I-iaAaFC-A6UfL`
**Redirect URI:** `https://oauth.yandex.ru/verification_code`

**Эндпоинты:**
- Отчёты: `/stat/v1/data/`
- Управление: CRUD счётчиков, целей через API управления

**Лимиты:** 30 запр/с с IP, 5000 запр/сутки, 200 запр/5мин для отчётов

**Документация:**
- Общее: https://yandex.ru/dev/metrika/ru/
- Квоты: https://yandex.ru/dev/metrika/ru/intro/quotas

**Статус:** не подключён, данные не выгружаются

### 1b. CRM Export API (Bitrix24, для ИИ-агентов)
- **Эндпоинт:** `https://24.uprav.ru/web_services/crm/export.php`
- **Secret key:** `14b0fc053c141e47a5974b3859f5753f`
- **Метод:** POST, Content-Type: `application/x-www-form-urlencoded`
- **Параметры:** `secret`, `action` (getDeals|getFormats|getDirections|getUserFieldsCrm), `data` (массив)
- **Документация:** `projects/clover-web/data/export-api-docs.md`
- **Особенности:**
  - Безопасная пагинация (limit ≤ 50, offset ≤ 5000)
  - Обогащение: название воронки, стадии, формата, направления, контакт, компания
  - WITH_PRODUCTS=Y — точечно для товаров/модулей (limit ≤ 5)
  - SELECT: безопасный набор, `*` и `UF_*` запрещены
- **Статус:** ✅ подключён, работает с 06.06.2026

## 🎯 Важное правило: работаем только на drop-dashboard-new

**С 08.06.2026 работаем ТОЛЬКО на http://94.183.233.92:3000/drop-dashboard-new/**
Не трогать старый drop-dashboard ни в коем случае.

**Пока Ольга не скажет "возвращайся" — не переключаться обратно.**
Если есть сомнения — переспросить, прежде чем что-то менять.

**Порядок работы:**
1. Ольга говорит задачу
2. Уточнить: "на drop-dashboard-new?"
3. Если да — делать там
4. Если нет — спросить "а где?"

## 🚫 Правило: не додумывать, только прямые указания

**Правки дашборда — только по прямым указаниям Ольги.**
- Не предлагать изменений, не додумывать, не делать лишнего
- Не торопиться
- Если Ольга хочет обсудить — она скажет: «давай подумаем», «как ты думаешь?», «предложи варианты»
- Это работает только в чате (обсуждение идей)
- Все изменения кода/дашборда — только когда Ольга прямо сказала что делать
- Если непонятно — переспросить, а не делать

## 📐 Правило: артефакты и выверка аналитики

**Моя задача — быть компьютером, который считает точно по правилам.**

1. **Выводить артефакты** — все аномалии показывать отдельным блоком:
   - Отрицательная длительность (UF_DATE_PAY_1C < DATE_CREATE) — оплата раньше создания
   - Возвраты (UF_DATE_PAY_1C + LOSE + >0)
   - Сделки с оплатой «в работе» (UF_DATE_PAY_1C + P)
   - WON без UF_DATE_PAY_1C
   - Технические сделки (WON + 0₽)

2. **Проверять данные** — перед каждым отчётом:
   - Все ли сделки соответствуют правилам?
   - Есть ли аномалии, которые искажают картину?
   - Что не стыкуется и рушит логику?

3. **Точность**:
   - Не гадать, не прикидывать — только проверенные цифры
   - Если сомневаюсь — переспросить или показать артефакт
   - Не додумывать логику расчёта

4. **Новые данные** — при каждом обновлении (новый месяц/неделя/сделки):
   - Пересчитать всё по правилам
   - Показать артефакты
   - Указать что не соответствует правилам и требует проверки

## 📋 SESSION.md — журнал сессии (утверждено 09.06.2026)

**Система ведения журнала, утверждённая Ольгой:**

1. **Получил задачу → записал в SESSION.md** → потом отвечаю. Фиксация, не в голове.
2. **Начал делать → пишу «начал, жди»** или промежуточный результат. Никакой тишины.
3. **Каждую правку — запись.** Что поменял, в каком файле, почему.
4. **Закончил → итог.** Что сделано, что осталось.
5. **При завершении сессии** → SESSION.md переезжает в `memory/YYYY-MM-DD.md`, ключевые моменты — в этот файл.
6. **Вся переписка важна** — записывать всё, что сказано, кроме простоев и ошибок.

## 🔒 Логика дашборда (09.06.2026, утверждено Ольгой, не терять!)

### Ключевые параметры
- **VALID_CATS:** только 0 (Sale), 8 (PreSale), 19 (КОМ Sale). Все остальные исключены.
- **MIN_OPP:** 11.0 ₽ (сделки 0–10 ₽ не считаются)
- **Источник:** CRM Export API + REST API дозагрузка UF_DATE_PAY_1C

### Лиды (is_all_lead)
- Только CAT_ID IN (0, 8, 19)
- НЕ тех.нулевые WON (SEM=S AND OPP < 11)
- НЕ WON-копии в PreSale(8) и КОМ(19) — это дубли при переходе в Sale
- **Сейчас: 4 493**

### MQL / Квал. лиды (is_qual_lead)
- CAT_ID IN (0, 19) — PreSale не входит
- **Sale (0):** стадии MQL+ (все кроме NEW, Аларм, Взят, Консультирование) + WON считается!
- **КОМ (19):** все кроме WON-копий и отказов (F)
- **Сейчас: 2 961 (65.9% от лидов)**

### Поступления
- UF_DATE_PAY_1C есть + OPPORTUNITY >= 11 + CAT IN (0,8,19)
- SEM не проверяем (любые стадии с оплатой считаем)
- pros/cur — строго по дате оплаты get_pay_date().isocalendar()
- **Сейчас: 74 927 954 ₽ / 861 сд.**

### Конверсия
- Конв. к лидам = WON / Все лиды × 100% = 19.2%
- Конв. к MQL = WON / MQL × 100% = 29.1%

### Цвета строк KPI
- ИТОГ: тёмно-синий (#1f2a44)
- ООМ: бирюзовый (#00bcd4)
- КОМ: фиолетовый (#9C27B0)

### Воронка продаж — 5 этапов

#### Sale воронка (категория 0)

**MQL (is_qual_lead):**
```
CAT_ID IN (0, 19)  # PreSale не входит

Sale (0): стадии MQL+ (все, кроме NEW/Аларм/Взят/Консульт)
  ВКЛЮЧАЯ WON (Ольга: WON в Sale считается MQL)
  
КОМ (19): все стадии, кроме WON-копий и LOSE (отказов)
```

Стадии ДО MQL (исключаются):
  NEW            — Исходный лид
  UC_1YW3V2      — Аларм/Распределение
  UC_STZB49      — Взят в работу
  UC_838R2R      — Консультирование

Стадии MQL+ в Sale:
  UC_4RJOR4 — Маркетинговый лид (MQL)
  DETAILS   — Лид для продажи (SQL)
  PROPOSAL  — Счёт отправлен
  2         — Постоплата
  6         — Частично оплачен
  WON       — Счёт оплачен
  LOSE      — Закрыто и не реализовано
  UC_F2YC3N — Предзакрытие в отказ
  UC_VKPN0N — Приоритет
  UC_W6SCHG — Следующий год
  UC_670ME2 — Возвращен в работу

**SQL (Sale):**
  Точно: DETAILS, PROPOSAL, 2, 6, WON
  С датой счёта (UF_CRM_1753272713011): LOSE, UC_F2YC3N, UC_W6SCHG, UC_670ME2, UC_VKPN0N
  
#### КОМ воронка (категория 19)

**MQL:** все стадии категории 19, кроме WON (копии в Sale) и LOSE (отказы)

**SQL:**
  Точно: C19:EXECUTING (КП направлено), C19:UC_C670BC (ОС получена), C19:UC_I443UQ (КП согласовано)
  С расчётом (UF_CRM_5D133690E1): C19:UC_ALOZ6B (На разбор), C19:UC_W4ML6H (Перенос сроков), C19:LOSE

### Архитектура (не менять!)
- **Все дашборды на порту 3000** через clover-web (sub-apps)
- **Работаем ТОЛЬКО на drop-dashboard-new** (старый не трогать)
- /drop-dashboard-new/ — управленческий дашборд (нoвая логика)
- API: /api/data/new, /api/artifacts, /api/forecast
- Скрипты: analyze_new.py (единственный), fetch_refresh.py, fetch_pay_dates.py
- config.py: MIN_OPP = 11.0
- merge.py НЕ ЗАПУСКАТЬ (портит данные)
