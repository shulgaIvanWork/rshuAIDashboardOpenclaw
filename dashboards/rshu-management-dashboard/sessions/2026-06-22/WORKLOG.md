# WORKLOG — 2026-06-22

**Участники:** Иван (руководитель), Клевер

---

## 06:24 UTC — Логика таблицы Воронка

Иван попросил объяснить логику таблицы «Воронка (фиксация состояния в периоде с учетом переходящего остатка, без учета даты создания)» из rshu-dashboard.

**Что сделано:**
- Детально разобрана функция `aggregateWithCarryOver()` в server.js
- Объяснены 12 колонок, потоковая цепочка, переходящий остаток
- Показано, что сделка фиксируется по дате СОБЫТИЯ (оплаты/проигрыша), а не создания

**Решение:** ✅ информация предоставлена

---

## 06:34 UTC — Полная перевыгрузка vs быстрая догрузка

Иван спросил про разницу.

**Что сделано:**
- Объяснено: полная выгрузка `fetchAllDeals()` — 5–15 мин, 20к+ сделок
- Быстрой догрузки в коде нет — только полная перевыгрузка
- Инкрементальная догрузка по DATE_MODIFY — не реализована

**Решение:** ✅ для обновления — кнопка «Обновить»

---

## 06:47 UTC — Сравнение 3-х мест по корп. обучению (не тот дашборд!)

Сначала Иван спросил про rshu-dashboard. Разобрал расхождения между KPI, форматами, типами.

**Проблема:** смотрел не тот дашборд.

**Решение:** ✅ переключились на rshu-management-dashboard

---

## 07:11–07:18 UTC — Расхождения в rshu-management-dashboard

Сравнил 3-й ряд КПЭ (КОМ), график форматов, график типов обучения.

**Выявлено:**
1. `is_kom_deal()` — 5 признаков через OR (галочка, формат, направление, категория=19, тип)
2. График форматов — группировка по `UF_FORMAT` → КОМ-сделки могут быть в других форматах
3. График типов — группировка по `UF_CRM_1765896709800` → у КОМ-сделок может быть пусто

**Запущен скрипт find_discrepancies.py (оставлен в scripts/)**

**Цифры на живых данных:**
- 237 КОМ-сделок, 66 090 960 ₽
- 43 сделки (15 006 601 ₽) без UF_FORMAT=КОМ — не попадают в график форматов
- 20 сделок (6 108 420 ₽) без типа=КОМ — не попадают в график типов
- 9 сделок (1 869 300 ₽) — нет ни в одном графике, только в 3-м ряду

**Решение:** ✅ данные предоставлены

---

## 07:22 UTC — Исправление 6-й карточки КПЭ

**app.js:** Добавлен второй ряд «Оплачено сделок» под суммой поступлений.

---

## 07:22–07:31 UTC — Цикл сделки: баг в понедельных данных

**Проблема:** ИТОГ и ООМ показывали одинаковый цикл (29.8 дн).

**Найден баг в analyze_new.py (строки 489–496):**
1. `durs` заполнялся только для ООМ-сделок → `avg_dur` ИТОГ = `avg_dur` ООМ
2. Длительность считалась по `CLOSEDATE`, а не `UF_DATE_PAY_1C`

**Исправление:**
- `durs` теперь включает все сделки (ООМ + КОМ)
- Длительность считается по `PAY_DT` (UF_DATE_PAY_1C)

**После исправления:**
- ИТОГ: 29.2 дн
- ООМ: 27.3 дн
- КОМ: 82.7 дн

**Закоммичено:** `657e971`

---

## 07:36–08:57 UTC — Конверсия: расхождение agg.json vs фронт

**Проблема:** agg.json показывал won_relevant_cnt=892, фронт (из понедельных) — 884.

**Причина:** `metrics()` не проверяла `PAY_DT.year == YEAR`. 8 сделок с `UF_DATE_PAY_1C` в 2025, созданных в 2026, попадали в YTD 2026.

**Решение:**
- Добавлен фильтр `r["PAY_DT"].year == YEAR` в `metrics()`
- По правилу Ивана: «считать в периоде оплаты»

**После исправления:**
- metrics: 884 / 75 045 032 ₽ ✅ сходится с понедельной
- Конверсия: 18.7% / 29.3% ✅ сходится с фронтом

**Закоммичено:** `d8970d8`

---

## Итог дня

| Правка | Файл | Коммит |
|--------|------|--------|
| «Оплачено сделок» в 6-ю карточку | app.js | 657e971 |
| Цикл: durs включает КОМ, PAY_DT вместо CL | analyze_new.py | 657e971 |
| metrics: фильтр PAY_DT.year == YEAR | analyze_new.py | d8970d8 |
