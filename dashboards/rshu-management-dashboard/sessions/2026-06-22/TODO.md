# TODO — 2026-06-22

## Сделать (когда Иван вернётся)

- [ ] **Быстрая догрузка сделок** — инкрементальное обновление по DATE_MODIFY вместо полной перевыгрузки
- [ ] **Унификация корп.обучения** — привести графики форматов и типов обучения к единой логике с is_kom_deal()
- [ ] **Кнопка перезапуска pipeline** — если `run_full.py` завис, нужен принудительный сброс (сейчас есть `/api/refresh/reset`)
- [ ] **Фильтр даты для длительности** — если пользователь выбирает диапазон, avg_close_days_won пересчитывается взвешенно по неделям. При пустых неделях может деление на ноль
- [ ] **Артефакты: сделки с PAY_DT не в 2026** — добавить в /api/artifacts новый блок «Оплата не в том году» (8 сделок, 672 400 ₽)
