# WORKLOG — Сессия 16.06.2026 (v2, обновлено 17.06)

**Участники:** Ольга (telegram), Clover
**Канал:** Telegram direct
**Проект:** rshu-management-dashboard (+ drop-dashboard)

---

## 11:40 — Старт сессии
Ольга: что делаешь? Обсуждение текущего состояния дашборда.

## 11:50 — Структура папки rshu-management-dashboard
Вывел структуру. Работаем через rshu-management-dashboard.

## 11:53 — Сравнение инструкций
Сравнил `Актуальная_инструкция_16_06_2026.md` и `Инструкция_к_управленческому_дашборду.md`.
Вывод: работать по новой инструкции, старая содержит устаревшие пути (drop-dashboard-new).

## 12:49 — Проблема: ООМ = ИТОГ
- **Ольга:** строка 2 (ООМ) равна строке 1 (ИТОГ), КОМ считается отдельно
- **Причина:** frontend в renderFilteredData() перезаписывал oom_ytd через sumField('postupleniya') — те же данные, что и для ИТОГ
- **Файл:** public/index.html, строки 254-255, 271-272, 282
- **Исправление:** переключил на oom_postupleniya/oom_won_cnt в buildFilteredData()

## 12:58 — Решение: OOM-поля в analyze_new.py
- Добавил в weekly: oom_postupleniya, oom_won_cnt, oom_leads, oom_mql (симметрично КОМ)
- Фронт переключил на эти поля
- **Результат:** данные в agg.json корректные (ИТОГ ≠ ООМ)

## 13:58 — Баг: server.js перезаписывает OOM
- **Файл:** server.js — 3 блока с `oom_ytd = {...ytd}`
- Один в loadCache(), второй в загрузке кэша (дубль), третий в /api/data/new
- Убрал все — теперь OOM данные только из agg.json

## 14:03 — Медиана ООМ = 55 300 (должна быть 55 000)
- **Причина:** server.js копировал ytd (55 300) в oom_ytd
- **Исправлено:** убрал перезапись

## 14:22 — Инструкция
- Создал `docs/Расчёт_KPI_3_строки_7_карточек.md`

## 16:21 — Новая stacked bar воронка
- **Задача:** заменить старую воронку (5 этапов, stacked) на новую с правильной логикой
- **Логика:** 6 сегментов по событиям на неделе: Оплата→Счёт→SQL→MQL→НеКвал→Отказы
- **analyze_new.py:** stack_pay/inv/sql/mql/nq/rej + суммы
- **index.html:** новый Chart.js stacked bar

## 16:27 — КОМ в счёт и оплату
- **Проблема:** invoice_cnt и oplata не включали КОМ
- **Исправлено:** убрал `not r["IS_KOM"]` из invoice, oplata теперь += 1 для всех

## 16:35 — Дозагрузка UF_CRM_1753272713011
- Export API не отдаёт дату счёта
- Добавил в fetch_kom_enrich.py: UF_CRM_1753272713011, MOVED_TIME, PREVIOUS_STAGE_ID

## 17:03 — Логика стека (6 сегментов)
- **Ольга уточнила:**
  - MQL/SQL = проверяем "была в статусе" на неделе
  - НеКвал = NEW/Аларм/Взят/Консульт + PreSale в работе
  - Одна сделка = одно место в стеке
  - Отказы = по CLOSEDATE на неделе

## 17:20 — Цвета
- Оплата — зелёный
- MQL — голубой

## 17:24 — Проблема: MQL слишком много (~800/нед)
- **Причина 1:** C19:LOSE (402) попадали в MQL — исправлено: убрал C19:LOSE из MQL
- **Причина 2:** C8:WON-копии (5625) попадали в SQL — исправлено: closed_before проверка
- **Ошибка в коде:** seg=None в блоке closed_before, но след. блок `if seg is None` перезаписывал
- **Исправлено:** переписал на if/else вместо seg=None

## 17:47 — Разделение стадий стека и KPI
- **Ольга:** MQL+ стадии из KPI не работают для стека
- **Исправлено:** _is_mql_stage (стек): Sale — только UC_4RJOR4, PreSale — False, КОМ — всё кроме WON/LOSE/EXECUTING
- _is_sql_stage (стек): убрал WON, только DETAILS/PROPOSAL/2/6 + КОМ EXECUTING

## 17:52 — Два стека: продажи и маркетинг
- **Ольга:** нужен второй стек — созданные на неделе × текущий статус
- Стек 1: все активные сделки на неделе
- Стек 2: созданные на неделе, статус на сегодня
- **analyze_new.py:** stack2_* поля

## 17:59 — Система файлов для фиксации
- **Ольга:** нужно фиксировать наработки в процессе
- **Файлы:** WORKLOG.md, DECISIONS.md, CHANGELOG.md, TODO.md

## 18:20 — PreSale в НеКвал
- **Ольга:** PreSale должен быть в НеКвал, не в MQL
- **ПРАВКА:** _is_mql_stage для cat 8 → return False

## 18:22 — WON/C8:WON excluded
- **Проблема:** 41 WON + 45 C8:WON (CLOSED=Y) попадали в НеКвал
- **ПРАВКА:** проверка won_closed → excluded

## 18:24 — UC_W6SCHG (Следующий год) = MQL
- **Ольга:** это квалифицированный лид
- **ПРАВКА:** _is_mql_stage += "UC_W6SCHG"

## 18:32 — Только стек 2
- **Ольга:** работаем только со стеком 2
- Вернул полные KPI-стадии для стека 2 (_is_sql2/_is_mql2)

## 18:36 — Тултип вместо datalabels
- Убрал datalabels со стека 2, оставил только тултип

## 18:38 — "Оба стека неверны"
- **Ольга:** да все равно неверны оба стека
- Статус: требуется уточнение что именно не так

## 18:39 — Фиксация
- Обновлены WORKLOG, DECISIONS, CHANGELOG, TODO

---

## 17.06.2026 — Аудит и документирование (v2)

### Найденные проблемы:

**1. WON/LOSE не переименованы полностью**
- В rshu-management-dashboard/index.html: заголовки «WON vs LOSE» и лейблы «Выиграно/Проиграно» в ch_cnt
- Исправлено: «Оплаты vs Отказы» везде

**2. Нет очистки графиков в renderPageMainNew()**
- При повторном входе на вкладку графики накладывались — Chart.js ошибка
- Исправлено: добавлен `destroyNewCharts()` + `chartInstancesNew = {}`

**3. Нет тултипа с % в newChWl**
- Был только в основной вкладке, в новой тултипа не было
- Исправлено: добавлен tooltip с долей %

**4. Сервер на 127.0.0.1**
- clover-web слушает только localhost (безопасно, т.к. nginx проксирует)

### Создано:
- `docs/Инструкция_по_графикам_и_таблицам.md` — полное описание каждого графика
- Файлы сессии v2
