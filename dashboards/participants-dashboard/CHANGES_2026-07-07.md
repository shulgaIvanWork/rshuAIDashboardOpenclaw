# Доработки participants-dashboard — 07.07.2026

## 1. Исключение КОМ
- `isKomDeal()` добавлен в `candidateDeals` и `contactTrainingDeals`
- КОМ-сделки не попадают в дашборд и не участвуют в расчётах

## 2. Направление (новая колонка)
- Источник: `UF_CRM_1498466811` на сделке
- Маппинг: справочник `directions` из `dicts.json`
- Если не задано — прочерк

## 3. Программа (исправлена логика)
- Теперь берётся из `product_name` товара (тема программы)
- Убран приоритет `original_name` (названия конкретных модулей)
- Пример: вместо «Календарное планирование...» → «Руководитель строительного проекта»

## 4. Даты и длительность (агрегированы)
- Модули одной программы на неделе группируются по `product_id`
- Показывается: минимальная `date_start` — максимальная `date_end`
- Количество дней = количество уникальных дат модулей

## 5. Участник (новая колонка)
- `UF_CRM_1477555902` (boolean): Да / Нет / —
- Добавлен в SELECT bitrix-rest.js и bitrix-export.js

## 6. Скидка, % (новая колонка)
- `UF_DISCOUNT` — скидка из счёта (в процентах)
- Добавлен в SELECT bitrix-rest.js и bitrix-export.js

## 7. Статус счета (новая колонка)
- Создан `lib/fetch-invoices.js` — выгрузка инвойсов через `crm.invoice.list` (batch)
- Статусы: Черновик / Отправлен / Принят 1С / Частично оплачен / Оплачен / Закрыт успешно / Отклонён
- Сохраняется в `cache/invoices.json` (5 217 шт.)
- Добавлен Шаг 6 в index.js

## 8. Последнее обучение от компании (новая колонка)
- По `COMPANY_ID` собираются все оплаченные сделки компании
- Исключается текущая сделка
- Показывается дата последнего оплаченного обучения

## 9. Выгрузка данных
- `npm run fetch` выполнен: 28 773 сделок, 20 350 контактов, 5 217 инвойсов

## Что не доделано (на завтра)
- **Порядок колонок** — выстроить логичную последовательность
- **Регионы** — отложено (текущая логика через ADDRESS_CITY работает)
