# Сессия 26.06.2026 — participants-dashboard

## MBA/MMBA рейтинг
- 5 групп: Micro MBA, MBA Эксперт, MBA Лидер, Mini MBA: Классический, Mini MBA: Специализация
- Определение только по названию (UF_CRM_1498466811 не нужен)
- `эксперт`/`micro` только с `mba`/`mmba` в названии

## Форматы
- **Очно** 🟢 (зелёный #2E7D32) — вместо "ООМ (Очное)"
- **Онлайн** 🔵 (синий #1976D2) — вместо "ОМ (Онлайн)"
- СДО исключён из расчётов (fsplit, fmt_keys, недельная агрегация)
- detectFormat в server.js синхронизирован

## Статусы и цвета
- Статус "WON" → "Счёт оплачен" 🟢
- Названия цветов:
  - Счёт оплачен = зелёный #2E7D32
  - Постоплата = тёмно-зелёный #1B5E20
  - Счёт отправлен = фиолетовый #9C27B0
  - Частичная оплата = оранжевый #FF9800
- Заголовки: "26 неделя" вместо "W26", "Очно/Онлайн" вместо "ООМ/ОМ"
- WON в server.js stageLabel → "Счёт оплачен"

## Модули (даты обучения)
**Проблема:** дата участника определяется из названия сделки (regex), а нужно из дат модулей.

**Структура данных:**
- Export API: `WITH_PRODUCTS=Y` → `products_tree[].modules_ids`
- `modules_ids` — ID модулей (отдельные продукты каталога)
- REST API: `catalog.product.get?id=N` → `property223` (MODULE_DATE_START), `property224` (MODULE_DATE_END)
- У Mini MBA может быть 19+ модулей на разных неделях

**Текущее состояние:**
- `fetch_modules.py` существует, но использует `crm.deal.productrows.get` (возвращает 1 строку на сделку)
- Нужно переделать: Export API WITH_PRODUCTS→modules_ids + catalog.product.get
- `modules.json` уже загружен (1844 сделки, 1844 модуля) — старым способом
- Из 902 оплаченных в 2026: 663 с модулями, 445 с датами в 2026

**Данные:**
- TODAY = date.today() (раньше было захардкожено 2026-06-07)
- Данные скопированы из rshu-management (через deals_NEW.json)
- YTD основное: 75,339,482 ₽ (910 сделок)
