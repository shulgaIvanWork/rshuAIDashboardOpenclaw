# Bitrix24 вебхук (REST API)
BITRIX_BASE=https://your-domain.bitrix24.ru/rest/1/your_token/

# Секрет для подписи сессий clover-web (сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=change-me

# Отчётный год для аналитики. По умолчанию — текущий календарный.
# RSHU_YEAR=2026
